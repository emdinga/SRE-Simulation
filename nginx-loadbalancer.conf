http {
    upstream backend {
        server host.docker.internal:8081;
        server host.docker.internal:8082;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://backend;
        }
    }
}
